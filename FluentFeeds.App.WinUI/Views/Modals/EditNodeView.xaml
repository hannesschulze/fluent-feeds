<ContentDialog
	x:Class="FluentFeeds.App.WinUI.Views.Modals.EditNodeView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:vm="using:FluentFeeds.App.Shared.ViewModels.Modals"
	xmlns:views="using:FluentFeeds.App.WinUI.Views.Items.GroupSelection"
	d:DataContext="{d:DesignInstance Type=vm:EditNodeViewModel}"
	mc:Ignorable="d"
	Title="Edit an item"
	PrimaryButtonText="Save"
	PrimaryButtonClick="HandlePrimaryButtonClick"
	CloseButtonText="Cancel"
	DefaultButton="Primary">

	<StackPanel Orientation="Vertical" MinWidth="350" Spacing="12">
		<StackPanel Orientation="Vertical" Spacing="4">
			<TextBlock Text="Name" LineHeight="20" />

			<TextBox
				x:Name="NameInput"
				Text="{x:Bind ViewModel.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
		</StackPanel>

		<StackPanel Orientation="Vertical" Spacing="4">
			<TextBlock Text="Location" LineHeight="20" />

			<views:GroupSelectionView DataContext="{x:Bind ViewModel.GroupSelectionViewModel}" />
		</StackPanel>
	</StackPanel>

</ContentDialog>
