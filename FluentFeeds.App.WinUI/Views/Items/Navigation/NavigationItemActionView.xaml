<MenuFlyoutItem
	x:Class="FluentFeeds.App.WinUI.Views.Items.Navigation.NavigationItemActionView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:vm="using:FluentFeeds.App.Shared.ViewModels.Items.Navigation"
	xmlns:helpers="using:FluentFeeds.App.WinUI.Helpers"
	d:DataContext="{d:DesignInstance Type=vm:NavigationItemActionViewModel}"
	mc:Ignorable="d"
	Text="{x:Bind ViewModel.Title}"
	Icon="{x:Bind helpers:SymbolHelper.ToIconElementOrNull(ViewModel.Symbol)}"
	Command="{x:Bind ViewModel.Command}" />
