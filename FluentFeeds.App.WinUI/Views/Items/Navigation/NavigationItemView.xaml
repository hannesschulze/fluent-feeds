<NavigationViewItem
	x:Class="FluentFeeds.App.WinUI.Views.Items.Navigation.NavigationItemView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:vm="using:FluentFeeds.App.Shared.ViewModels.Items.Navigation"
	xmlns:helpers="using:FluentFeeds.App.WinUI.Helpers"
	d:DataContext="{d:DesignInstance Type=vm:NavigationItemViewModel}"
	mc:Ignorable="d"
	MenuItemsSource="{x:Bind ViewModel.Children}"
	Content="{x:Bind ViewModel.Title, Mode=OneWay}"
	Icon="{x:Bind helpers:SymbolHelper.ToIconElement(ViewModel.Symbol), Mode=OneWay}" />
